<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yourwebsite.guessthenumber">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GuessTheNumber">
        <activity android:name=".ui.activity_test" />
        <activity android:name=".ui.activity_game_view" />
        <activity android:name=".ui.activity_splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity_result" />
        <activity android:name=".ui.activity_question" />
        <activity android:name=".ui.activity_high_score" />
        <activity android:name=".ui.activity_itunes_results"/>
        <activity android:name=".ui.MainActivity" />
    </application>

</manifest>