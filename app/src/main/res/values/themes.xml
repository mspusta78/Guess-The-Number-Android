<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.GuessTheNumber" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowActionBar">true</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="defaultButtonStyle" parent="Widget.MaterialComponents.Button">
        <item name="cornerRadius">10dp</item>
        <item name="android:padding">15dp</item>
        <item name="backgroundTint">@color/black</item>
    </style>

    <style name="levelMaterialCard" parent="@style/Widget.MaterialComponents.CardView">
        <item name="android:layout_width">75dp</item>
        <item name="android:layout_height">75dp</item>
        <item name="cardCornerRadius">15dp</item>
        <item name="cardBackgroundColor">@android:color/holo_blue_dark</item>
        <item name="android:orientation">vertical</item>
    </style>

    <style name="numberMaterialCard" parent="@style/Widget.MaterialComponents.CardView">
        <item name="android:layout_width">55dp</item>
        <item name="android:layout_height">55dp</item>
        <item name="cardCornerRadius">15dp</item>
        <item name="cardBackgroundColor">@android:color/holo_blue_dark</item>
        <item name="android:orientation">vertical</item>
    </style>

    <style name="numberText" parent="@style/Widget.MaterialComponents.TextView">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:textAlignment">center</item>
        <item name="android:gravity">center</item>
        <item name="android:textSize">18dp</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="TextInputLayoutStyle" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox.Dense">
        <item name="boxStrokeColor">#fff</item>
        <item name="boxStrokeWidth">1dp</item>
    </style>

</resources>